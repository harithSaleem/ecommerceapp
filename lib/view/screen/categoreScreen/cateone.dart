import 'package:flutter/material.dart';

class Categorieone extends StatelessWidget {
  const Categorieone({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Soon'),
      ),
    );
  }
}
