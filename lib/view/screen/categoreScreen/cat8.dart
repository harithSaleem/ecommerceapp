import 'package:flutter/material.dart';

class Categorieeight extends StatelessWidget {
  const Categorieeight({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Soon'),
      ),
    );
  }
}
