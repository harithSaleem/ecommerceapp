import 'package:flutter/material.dart';

class Categoriesix extends StatelessWidget {
  const Categoriesix({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Soon'),
      ),
    );
  }
}
