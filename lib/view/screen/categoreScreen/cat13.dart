import 'package:flutter/material.dart';

class Categoriethr extends StatelessWidget {
  const Categoriethr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Soon'),
      ),
    );
  }
}
