import 'package:flutter/material.dart';

class Categoriefour extends StatelessWidget {
  const Categoriefour({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Soon'),
      ),
    );
  }
}
