import 'package:flutter/material.dart';

class Categoriefive extends StatelessWidget {
  const Categoriefive({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Soon'),
      ),
    );
  }
}
