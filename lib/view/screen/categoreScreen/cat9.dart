import 'package:flutter/material.dart';

class Categorienine extends StatelessWidget {
  const Categorienine({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Soon'),
      ),
    );
  }
}
