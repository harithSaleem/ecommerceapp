import 'package:flutter/material.dart';

class Categorieelvent extends StatelessWidget {
  const Categorieelvent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Soon'),
      ),
    );
  }
}
